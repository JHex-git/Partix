#ifndef COMMON_INCL
#define COMMON_INCL

layout(location=0) in bool alive;
layout(location=1) in float lifetime;
layout(location=2) in vec3 position;
layout(location=3) in vec3 velocity;

struct Particle
{
    bool alive;
    float lifetime;
    vec3 position;
    vec3 velocity;
};

layout(location=0) in Particle particle;
#endif // COMMON_INCL